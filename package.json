{
  "name": "operator",
  "scripts": {
    "applets:build": "cd applets && npm run build",
    "applets:sync": "node scripts/sync.js",
    "dev": "npm run applets:build && npm run applets:sync && cd web && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.7"
  }
}
